

export function reducer(state, action) {
    switch (action.type) {
        case "getList":

            return {
                ...state,

            }
        default:
            return state;
    }

}
